{
  "name": "atlas-function-types",
  "version": "1.0.0",
  "types": "./lib/main.d.ts",
  "scripts": {
    "doc": "npx typedoc lib",
    "publish": "npm run doc && gh-pages -d docs"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}
